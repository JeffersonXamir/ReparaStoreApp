<UserControl x:Class="ReparaStoreApp.WPF.Views.Users.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReparaStoreApp.WPF.Views.Users"
             mc:Ignorable="d" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Lista genérica -->
        <ContentControl Grid.Column="0" 
                        cal:View.Model="{Binding UserList}"
                        Margin="5"/>

        <!-- Detalles del usuario -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Detalles del Usuario" FontSize="16" FontWeight="Bold"/>

            <Grid Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ID:" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         Text="{Binding CurrentUser.Id, Mode=OneWay}" 
                         IsReadOnly="True" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Nombre:" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         Text="{Binding CurrentUser.Name}" 
                         IsEnabled="{Binding IsInEditOrCreationMode}"
                         Margin="5"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Margin="5"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         Text="{Binding CurrentUser.Email}" 
                         IsEnabled="{Binding IsInEditOrCreationMode}"
                         Margin="5"/>
            </Grid>

            <!--<Button Content="Guardar" 
                    cal:Message.Attach="[Event Click] = [SaveUser()]"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"/>-->
        </StackPanel>
    </Grid>
</UserControl>
